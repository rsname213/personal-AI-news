---
phase: 02-polish-and-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/digest.html.j2
  - pipeline/render.py
autonomous: true
requirements:
  - EMAIL-01
  - EMAIL-02
  - EMAIL-04
  - EMAIL-05
  - EMAIL-06
must_haves:
  truths:
    - "The rendered email HTML has all structural CSS inlined as style= attributes (Gmail-compatible)"
    - "The rendered HTML contains a <style data-premailer='ignore'> block with @media (prefers-color-scheme: dark) rules — not stripped by premailer"
    - "The email visual design matches Wispr Flow aesthetic: white card on light-gray background, Inter/system-ui font, generous whitespace, small uppercase section labels, no heavy borders or shadows"
    - "A section with zero articles is absent from the rendered email (no empty section header rendered)"
    - "Total rendered HTML size stays below 102KB"
    - "The <style> block remaining in the premailer output is under 8KB (Gmail style block limit)"
  artifacts:
    - path: "templates/digest.html.j2"
      provides: "Wispr Flow-inspired email template with dark mode @media block and data-premailer=ignore attribute"
      contains: "data-premailer=\"ignore\""
    - path: "pipeline/render.py"
      provides: "premailer transform called with keep_style_tags=True and strip_important=False"
      contains: "keep_style_tags=True"
  key_links:
    - from: "templates/digest.html.j2 (<style> block)"
      to: "premailer.transform()"
      via: "structural <style> gets inlined; data-premailer='ignore' style block is skipped"
      pattern: "data-premailer"
    - from: "pipeline/render.py"
      to: "premailer.transform"
      via: "keep_style_tags=True preserves remaining style blocks including dark mode @media"
      pattern: "keep_style_tags=True"
---

<objective>
Redesign the email template to a Wispr Flow-inspired minimal aesthetic and fix the premailer call so the dark mode `@media` block survives CSS inlining.

Purpose: Makes the digest visually polished (EMAIL-01, EMAIL-02), Gmail-safe (EMAIL-01, EMAIL-04), dark-mode-capable in Apple Mail/iOS Mail (EMAIL-05), and verifies empty section suppression (EMAIL-06).

Output: A fully rewritten `templates/digest.html.j2` with Wispr Flow design tokens + dark mode block, and a one-line fix to `pipeline/render.py` changing `transform(raw_html)` to `transform(raw_html, keep_style_tags=True, strip_important=False)`.
</objective>

<execution_context>
@/home/codespace/.claude/get-shit-done/workflows/execute-plan.md
@/home/codespace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-polish-and-resilience/02-RESEARCH.md

<interfaces>
<!-- Key types and current file contents the executor needs. No codebase exploration required. -->

From models.py:
```python
class SummarizedArticle:
    title: str
    url: str
    source_name: str
    source_category: str   # one of: "Personal Blogs", "WSJ", "The Information", "Anthropic"
    published_at: datetime  # aware datetime; use .strftime('%b %-d, %Y') for display
    summary: str
    why_it_matters: str
    summarization_failed: bool
```

From pipeline/render.py (current — to be modified):
```python
# Line 48 — THE ONLY LINE TO CHANGE:
inlined_html = transform(raw_html)
# Change to:
inlined_html = transform(raw_html, keep_style_tags=True, strip_important=False)
```

Template variables available in Jinja2 context:
- `date` — string like "March 1, 2026"
- `sections` — dict[str, list[SummarizedArticle]], already filtered (empty sections removed by render.py line 42)

Current template structure (to be REPLACED — not incrementally modified):
```
digest.html.j2: 63-line Phase 1 template, single <style> block, no dark mode, no data-premailer="ignore"
```
</interfaces>

Design tokens (from RESEARCH.md — use these exactly):
| Token | Light value | Dark value |
|-------|-------------|------------|
| Outer bg | #f5f5f5 | #121212 |
| Card bg | #ffffff | #1e1e1e |
| Text primary | #111111 | #eeeeee |
| Text secondary / meta | #888888 | #888888 |
| Text body | #333333 | #cccccc |
| Why it matters text | #555555 | #aaaaaa |
| Why it matters border | #dddddd | #444444 |
| Section label | #999999 | #666666 |
| Section divider | #e8e8e8 | #333333 |
| Footer text | #aaaaaa | #555555 |
| Footer border | #eeeeee | #2a2a2a |
| Link color | #111111 | #eeeeee |
| Font stack | -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign digest.html.j2 with Wispr Flow aesthetic + dark mode block</name>
  <files>templates/digest.html.j2</files>
  <action>
    Completely replace `/workspaces/personal-AI-news/templates/digest.html.j2` with the following structure. This is a full rewrite — do not incrementally patch the existing file.

    REQUIRED STRUCTURE (implement exactly as described):

    **Head section:**
    ```html
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <meta name="color-scheme" content="light dark">
      <meta name="supported-color-schemes" content="light dark">

      <!-- DARK MODE BLOCK — data-premailer="ignore" tells premailer to skip this block entirely -->
      <style data-premailer="ignore">
        @media (prefers-color-scheme: dark) {
          body { background-color: #121212 !important; }
          .outer-td { background-color: #121212 !important; }
          .content-td { background-color: #1e1e1e !important; }
          .header-title { color: #eeeeee !important; }
          .header-date { color: #777777 !important; }
          .section-label { color: #666666 !important; border-bottom-color: #333333 !important; }
          .article-title { color: #eeeeee !important; }
          .article-title:visited { color: #eeeeee !important; }
          .article-meta { color: #888888 !important; }
          .article-summary { color: #cccccc !important; }
          .article-why { color: #aaaaaa !important; border-left-color: #444444 !important; }
          .article-no-summary { color: #888888 !important; }
          .footer { color: #555555 !important; border-top-color: #2a2a2a !important; }
        }
      </style>

      <!-- STRUCTURAL STYLES — inlined by premailer into style="" attributes -->
      <style>
        body {
          margin: 0; padding: 0;
          background-color: #f5f5f5;
          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
          -webkit-font-smoothing: antialiased;
        }
        .outer-td { padding: 32px 16px; background-color: #f5f5f5; }
        .content-td { background-color: #ffffff; padding: 40px 36px; }
        .header-title {
          font-size: 20px; font-weight: 700; color: #111111;
          margin: 0 0 6px 0; letter-spacing: -0.01em;
        }
        .header-date { font-size: 13px; color: #888888; margin: 0 0 40px 0; }
        .section-label {
          font-size: 10px; font-weight: 600; letter-spacing: 0.12em;
          text-transform: uppercase; color: #999999;
          border-bottom: 1px solid #e8e8e8; padding-bottom: 8px;
          margin: 36px 0 18px 0;
        }
        .article-title {
          font-size: 15px; font-weight: 600; color: #111111;
          text-decoration: none; display: block;
          margin-bottom: 5px; line-height: 1.4;
        }
        .article-meta { font-size: 12px; color: #888888; margin: 0 0 10px 0; }
        .article-summary {
          font-size: 14px; color: #333333;
          line-height: 1.65; margin: 0 0 10px 0;
        }
        .article-why {
          font-size: 13px; color: #555555;
          border-left: 3px solid #dddddd; padding-left: 12px;
          margin: 0 0 28px 0; line-height: 1.55;
        }
        .article-no-summary {
          font-size: 13px; color: #888888;
          font-style: italic; margin: 0 0 28px 0;
        }
        .footer {
          font-size: 11px; color: #aaaaaa;
          margin-top: 40px; border-top: 1px solid #eeeeee; padding-top: 16px;
        }
      </style>
    </head>
    ```

    **Body structure (table-based, Gmail-safe — NO flexbox, NO grid, NO box-shadow, NO border-radius):**
    ```html
    <body>
      <table width="100%" cellpadding="0" cellspacing="0" border="0">
        <tr>
          <td class="outer-td" align="center">
            <table width="600" cellpadding="0" cellspacing="0" border="0" style="max-width:600px;width:100%;">
              <tr>
                <td class="content-td">

                  <p class="header-title">AI Briefing</p>
                  <p class="header-date">{{ date }}</p>

                  {% for category, articles in sections.items() %}
                  {% if articles %}
                  <p class="section-label">{{ category }}</p>
                  {% for article in articles %}
                  <a href="{{ article.url }}" class="article-title">{{ article.title }}</a>
                  <p class="article-meta">
                    {{ article.source_name }}&nbsp;&middot;&nbsp;{{ article.published_at.strftime('%b %-d, %Y') }}
                  </p>
                  {% if not article.summarization_failed %}
                  <p class="article-summary">{{ article.summary }}</p>
                  <p class="article-why"><strong>Why it matters:</strong> {{ article.why_it_matters }}</p>
                  {% else %}
                  <p class="article-no-summary">Summary unavailable &mdash;
                    <a href="{{ article.url }}" style="color:#666666;">read article</a>
                  </p>
                  {% endif %}
                  {% endfor %}
                  {% endif %}
                  {% endfor %}

                  <p class="footer">Generated {{ date }}&nbsp;&middot;&nbsp;Personal AI Briefing</p>

                </td>
              </tr>
            </table>
          </td>
        </tr>
      </table>
    </body>
    ```

    CONSTRAINTS to enforce:
    - NO `border-radius` (stripped by Gmail)
    - NO `box-shadow` (stripped by Gmail)
    - NO `display: flex` or `display: grid` (stripped by Gmail)
    - NO `@import` or `<link rel="stylesheet">` (stripped by Gmail)
    - NO Google Fonts (system font stack only)
    - Dark mode `<style>` block MUST have `data-premailer="ignore"` attribute
    - The `{% if articles %}` guard inside `{% for category, articles %}` is REQUIRED for EMAIL-06 (suppresses empty sections)
    - article.url must use `{{ article.url }}` directly in href — Jinja2 autoescape is on, URLs will be properly escaped

    Verify the dark mode style block stays compact (count rules manually — target ≤ 15 declarations inside the @media block).
  </action>
  <verify>
    <automated>cd /workspaces/personal-AI-news && python -c "
from jinja2 import Environment, FileSystemLoader
from datetime import datetime, timezone
env = Environment(loader=FileSystemLoader('templates'), autoescape=True)
t = env.get_template('digest.html.j2')
html = t.render(sections={}, date='March 1, 2026')
assert 'data-premailer=\"ignore\"' in html, 'Missing data-premailer=ignore'
assert '@media (prefers-color-scheme: dark)' in html, 'Missing dark mode @media'
assert 'AI Briefing' in html, 'Missing header'
print('Template renders OK — empty sections:', len(html), 'bytes')
"</automated>
  </verify>
  <done>Template renders without Jinja2 errors. Contains data-premailer="ignore" attribute. Contains @media (prefers-color-scheme: dark) block. Renders correctly with empty sections dict (EMAIL-06 edge case — no section headers shown). Renders correctly with sample articles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix premailer call in render.py and verify output quality</name>
  <files>pipeline/render.py, tests/test_render.py</files>
  <behavior>
    - render_email([]) renders without error and returns (html_str, text_str) where html_str contains "AI Briefing"
    - render_email([]) returns html where @media (prefers-color-scheme: dark) is present in the inlined output (dark mode block survived premailer)
    - render_email([]) returns html where at least one style="" attribute exists on a table or td element (CSS was inlined)
    - render_email(articles_with_failed_summary) renders the "Summary unavailable" fallback without crashing
    - render_email(articles) returns html with total byte size < 102_000
    - The remaining <style> block in the premailer output is < 8192 characters (Gmail style block limit)
    - Empty sections passed to render_email produce no section-label elements in the output (EMAIL-06)
  </behavior>
  <action>
    STEP 1 — Write tests first in `/workspaces/personal-AI-news/tests/test_render.py`.
    Create the tests directory and `tests/__init__.py` if they do not exist (they may already exist from Task 1 of plan 02-01 — check first).

    Test helpers needed:
    ```python
    from datetime import datetime, timezone
    from models import SummarizedArticle

    def make_article(title="Test Article", url="https://example.com/test",
                     source_name="Test Blog", source_category="Personal Blogs",
                     summary="A test summary.", why_it_matters="It matters.",
                     summarization_failed=False):
        return SummarizedArticle(
            title=title, url=url, source_name=source_name,
            source_category=source_category,
            published_at=datetime(2026, 3, 1, 10, 0, tzinfo=timezone.utc),
            summary=summary, why_it_matters=why_it_matters,
            summarization_failed=summarization_failed,
        )
    ```

    STEP 2 — Modify `/workspaces/personal-AI-news/pipeline/render.py`.

    Change ONLY line 48 (the transform call). Replace:
    ```python
    inlined_html = transform(raw_html)  # Inline all CSS for Gmail compatibility
    ```
    With:
    ```python
    # keep_style_tags=True preserves the dark mode @media block in <head>
    # strip_important=False preserves !important in @media overrides
    inlined_html = transform(raw_html, keep_style_tags=True, strip_important=False)
    ```

    No other changes to render.py.

    STEP 3 — After tests pass, add a style block size check print statement after the transform call:
    ```python
    import re as _re
    _style_blocks = _re.findall(r'<style[^>]*>(.*?)</style>', inlined_html, _re.DOTALL)
    _style_size = sum(len(b) for b in _style_blocks)
    if _style_size > 7000:
        print(f"[WARN] Style block size {_style_size} chars — approaching 8KB Gmail limit")
    ```
    Add this AFTER the transform line, BEFORE the plain-text fallback section. Use a leading underscore to avoid polluting the module namespace.
  </action>
  <verify>
    <automated>cd /workspaces/personal-AI-news && python -m pytest tests/test_render.py -v 2>&1 | tail -25</automated>
  </verify>
  <done>All test_render.py tests pass. render.py calls transform with keep_style_tags=True and strip_important=False. Rendered HTML contains @media (prefers-color-scheme: dark). HTML size is well under 102KB. Style block size check is present.</done>
</task>

</tasks>

<verification>
Run all render tests:
```bash
cd /workspaces/personal-AI-news && python -m pytest tests/test_render.py -v
```

Verify dark mode block survives premailer (end-to-end render check):
```bash
cd /workspaces/personal-AI-news && python -c "
from pipeline.render import render_email
html, text = render_email([])
print('HTML size:', len(html), 'bytes')
assert '@media (prefers-color-scheme: dark)' in html, 'FAIL: dark mode stripped by premailer'
assert 'style=' in html, 'FAIL: no inlined CSS found'
assert 'data-premailer' not in html, 'FAIL: data-premailer attribute should be stripped from output'
print('PASS: dark mode block preserved, CSS inlined')
"
```

Verify EMAIL-06 edge case — empty sections produce no section headers:
```bash
cd /workspaces/personal-AI-news && python -c "
from pipeline.render import render_email
html, _ = render_email([])
assert 'section-label' not in html or html.count('section-label') == 0, 'FAIL: empty section header rendered'
print('PASS: no section headers in empty digest')
"
```

Verify HTML size under 102KB with sample articles:
```bash
cd /workspaces/personal-AI-news && python -m pytest tests/test_render.py -k 'size' -v
```
</verification>

<success_criteria>
- templates/digest.html.j2 is fully rewritten with Wispr Flow design tokens
- digest.html.j2 contains `<style data-premailer="ignore">` with @media dark mode block
- digest.html.j2 contains structural `<style>` block (without data-premailer attribute) for premailer inlining
- pipeline/render.py calls transform(raw_html, keep_style_tags=True, strip_important=False)
- All tests in tests/test_render.py pass
- Rendered HTML includes @media (prefers-color-scheme: dark) in output (survived premailer)
- Rendered HTML has inline style= attributes (CSS was inlined)
- HTML size well under 102KB
- Empty sections produce no section headers in rendered output (EMAIL-06 verified)
</success_criteria>

<output>
After completion, create `/workspaces/personal-AI-news/.planning/phases/02-polish-and-resilience/02-02-SUMMARY.md`
</output>
